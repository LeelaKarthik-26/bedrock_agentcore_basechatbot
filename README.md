# bedrock_agentcore_basechatbot

## Streamlit frontend

Run the app:

```powershell
streamlit run streamlit_app.py
```

The UI loads the existing `agent` from `00_langgraph_agent.py` and lets you chat with it in the browser.
